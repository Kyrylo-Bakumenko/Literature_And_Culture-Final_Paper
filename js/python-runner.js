let pyodide = null;

async function initializePyodide() {
    // We're not initializing Pyodide anymore since we're not running code
    console.log("Code execution disabled - only showing code samples");
}

function setupPythonCells() {
    // This function is now replaced by setupCodeToggle in code-toggle.js
    console.log("Python cell setup handled by code-toggle.js");
}
